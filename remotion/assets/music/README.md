# Optional background music

Place a **royalty-free / original** ambient track here as:

`ambient-placeholder.mp3`

Then set `useMusic = true` in `src/EyeQDemo.tsx`.

Do **not** add copyrighted commercial music.
The default render ships without music so licensing stays clean.
