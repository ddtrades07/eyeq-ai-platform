'use client';

/**
 * @deprecated Prefer `ScrollReveal` from `@/components/marketing/ScrollReveal`.
 * Kept as a thin alias so older imports keep working.
 */
export { ScrollReveal as FadeIn, ScrollStagger as Stagger } from '@/components/marketing/ScrollReveal';
